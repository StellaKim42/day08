# Studying git
Start to learn how to use git

# Start git wit bush
1. git init
2. git remote add origin <git url>
3. git branch -M <branch name>
4. git push -u origin <branch name>
5. git add .
6. git commit -m ""
7. git push
7. git push

# How to use git when collaborate
1. PLEASE PULL THE LATEST GIT BEFORE WORKING, or you may lose your data.
2. If there any comflicts, notity me.

# Create new branch
1. If you want to add new code, using branch.
2. You can create the new branch with below.
    git switch -c <new branch name>
3. You can check the current branch using the command below. The current branch will be marked with an asterisk (*).
    git branch

# merge the branch
1. Merge 2 branches using below code.
